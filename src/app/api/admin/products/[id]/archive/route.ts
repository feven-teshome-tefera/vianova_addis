import {NextRequest,NextResponse} from "next/server";import {ProductStatus} from "@prisma/client";import {z} from "zod";import {db} from "@/lib/db";import {apiError} from "@/lib/api";
const schema=z.object({archived:z.boolean()});export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const{archived}=schema.parse(await req.json());return NextResponse.json(await db.product.update({where:{id:(await params).id},data:{status:archived?ProductStatus.ARCHIVED:ProductStatus.DRAFT}}))}catch(e){return apiError(e,{context:"admin-archive-product",defaultMessage:"We couldn’t update this product’s archive status. Please try again.",notFoundMessage:"This product no longer exists. Refresh the product list."})}}
