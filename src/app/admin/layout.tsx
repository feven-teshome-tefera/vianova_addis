import {AdminShell} from "@/components/admin/shell";import {supabaseServer} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Layout({children}:{children:React.ReactNode}){const {data:{user}}=await (await supabaseServer()).auth.getUser();const name=(user?.user_metadata?.name as string|undefined)??user?.email?.split("@")[0]??"Admin";return <AdminShell userName={name}>{children}</AdminShell>}
