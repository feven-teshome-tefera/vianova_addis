import {NextResponse} from "next/server";import {db} from "@/lib/db";import {ProductStatus,Availability} from "@prisma/client";import {apiError} from "@/lib/api";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const p=await db.product.findFirst({where:{id:(await params).id,status:ProductStatus.PUBLISHED,availability:Availability.IN_STOCK},include:{category:true,media:true,sizes:{where:{isActive:true},orderBy:{position:"asc"}}}});if(!p)throw new Error("NOT_FOUND");return NextResponse.json(p)}catch(e){return apiError(e,{context:"storefront-product",defaultMessage:"We couldn’t load this product. Refresh the page and try again.",notFoundMessage:"This product is no longer available."})}}
