import {NextResponse} from "next/server";import {db} from "@/lib/db";import {apiError} from "@/lib/api";import {removeStoredMedia} from "@/lib/storage";import {supabaseServer} from "@/lib/supabase/server";
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const media=await db.productMedia.delete({where:{id:(await params).id}});await removeStoredMedia(await supabaseServer(),[media.url]);return new NextResponse(null,{status:204})}catch(e){return apiError(e,{context:"delete-product-media",defaultMessage:"We couldn’t remove this media file. Please try again.",notFoundMessage:"This media file has already been removed. Refresh the product page."})}}
