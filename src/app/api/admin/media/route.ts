import {NextRequest,NextResponse} from "next/server";import {mkdir,writeFile} from "node:fs/promises";import path from "node:path";import {MediaType} from "@prisma/client";import {apiError} from "@/lib/api";import {db} from "@/lib/db";
const allowed=new Set(["image/jpeg","image/png","image/webp","video/mp4","video/webm"]);export async function POST(req:NextRequest){try{const data=await req.formData(),file=data.get("file"),productId=data.get("productId");if(!(file instanceof File))return NextResponse.json({error:"Choose an image or video before uploading."},{status:400});if(typeof productId!=="string"||!productId)return NextResponse.json({error:"Save the product before uploading its media."},{status:400});if(!allowed.has(file.type))return NextResponse.json({error:"This file format is not supported. Use JPG, PNG, WebP, MP4, or WebM."},{status:415});const filename=`${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"-")}`;const dir=path.join(process.cwd(),"public","uploads");await mkdir(dir,{recursive:true});await writeFile(path.join(dir,filename),Buffer.from(await file.arrayBuffer()));const media=await db.productMedia.create({data:{productId,type:file.type.startsWith("video/")?MediaType.VIDEO:MediaType.IMAGE,url:`/uploads/${filename}`,filename,isPrimary:data.get("isPrimary")==="true"}});return NextResponse.json(media,{status:201})}catch(e){return apiError(e,{context:"upload-product-media",defaultMessage:"We couldn’t upload this file. Check the format and try again."})}}
