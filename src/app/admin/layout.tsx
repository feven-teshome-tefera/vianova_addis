import {AdminShell} from "@/components/admin/shell";import {db} from "@/lib/db";import {supabaseServer} from "@/lib/supabase/server";export const dynamic="force-dynamic";
export default async function Layout({children}:{children:React.ReactNode}){const [{data:{user}},settings]=await Promise.all([(await supabaseServer()).auth.getUser(),db.storeSettings.findUnique({where:{id:"default"},select:{storeName:true}})]);const name=(user?.user_metadata?.name as string|undefined)??user?.email?.split("@")[0]??"Admin";return <AdminShell userName={name} brand={settings?.storeName??"Via Nova"}>{children}</AdminShell>}
